# Domain configuration
DOMAIN=commitspace.com
EMAIL=vivekkannan6549@gmail.com

# Vite build-time environment (used during docker build)
VITE_API_URL=https://commitspace.com/api

# Note: Copy this file to 'docker.env' and update with your values
# The docker.env file is gitignored for security
